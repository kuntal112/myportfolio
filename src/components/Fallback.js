import React from "react";

const Fallback = () => {
  return (
      <div className="fallbackpage">
    <div className="loader">
      <h1>Loading...</h1>
    </div>
    </div>
  );
};

export default Fallback;
